{
  "name": "secrets",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building app...'",  
    "deploy": "bash ./deploy.sh",   
    "doppler-sync-secrets": "doppler secrets download --project=my-project --config=production --no-file"
  },
  "dependencies": {
    "axios": "^0.21.1"   
  }
}
